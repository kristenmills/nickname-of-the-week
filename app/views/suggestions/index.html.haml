%h1 Suggestions

.table-responsive
  %table.table.table-hover
    %thead
      %tr
        %th Name
        %th Description
        %th Submitter
        %th Create
        %th Delete
    %tbody
      - @suggestions.each do |suggestion|
        %tr{onclick: "window.location = \"#{edit_suggestion_path(suggestion)}\";"}
          %td= suggestion.name
          %td= suggestion.description
          %td= suggestion.submitter
          %td
            = link_to  nicknameify_suggestion_path(suggestion), method: :post do
              %i.icon-ok
          %td
            = link_to suggestion, :method => :delete, :data => { :confirm => 'Are you sure?' } do
              %i.icon-remove
